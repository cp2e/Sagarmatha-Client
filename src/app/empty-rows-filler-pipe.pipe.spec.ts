import { EmptyRowsFillerPipePipe } from './empty-rows-filler-pipe.pipe';

describe('EmptyRowsFillerPipePipe', () => {
  it('create an instance', () => {
    const pipe = new EmptyRowsFillerPipePipe();
    expect(pipe).toBeTruthy();
  });
});
